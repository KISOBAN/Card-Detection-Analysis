The Attributes folder contains all the attributes (Fire, Wind, Water, Dark etc) that are used for comparison when taking a snapshot of the input attribute
The SampleCards folder contains sample input images of random yugioh cards for testing
